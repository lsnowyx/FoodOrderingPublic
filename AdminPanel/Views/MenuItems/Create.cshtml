@model AdminPanel.Models.MenuItem.MenuItemViewModel

@{
    ViewData["Title"] = "Create Menu Item";
}

<h3>Create Menu Item</h3>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="form-group mb-2">
        @Html.LabelFor(m => m.Name)
        @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Name, "", new { @class = "text-danger" })
    </div>
    <div class="form-group mb-2">
        @Html.LabelFor(m => m.Description)
        @Html.TextAreaFor(m => m.Description, new { @class = "form-control", rows = 3 })
        @Html.ValidationMessageFor(m => m.Description, "", new { @class = "text-danger" })
    </div>
    <div class="form-group mb-2">
        @Html.LabelFor(m => m.Price)
        @Html.TextBoxFor(m => m.Price, new { @class = "form-control", type = "number", step = "0.01" })
        @Html.ValidationMessageFor(m => m.Price, "", new { @class = "text-danger" })
    </div>
    <div class="form-group mb-2">
        @Html.LabelFor(m => m.CategoryId, "Category")
        @{ var categoriesList = new SelectList((IEnumerable<AdminPanel.Models.Category.CategoryViewModel>)ViewBag.Categories, "Id", "Name"); }
        <input id="categorySearch" class="form-control mb-1" placeholder="Search categories..." />
        @Html.DropDownListFor(m => m.CategoryId, categoriesList, "-- select category --", new { @class = "form-control", id = "categorySelect" })
        @Html.ValidationMessageFor(m => m.CategoryId, "", new { @class = "text-danger" })
    </div>
    <div class="form-check mb-3">
        @Html.CheckBoxFor(m => m.IsAvailable, new { @class = "form-check-input" })
        @Html.LabelFor(m => m.IsAvailable, new { @class = "form-check-label" })
    </div>
    <button class="btn btn-primary">Create</button>
    <a class="btn btn-secondary" asp-action="Index">Cancel</a>
}

<script>
document.getElementById('categorySearch')?.addEventListener('input', function(e){
    const q = (e.target.value || '').toLowerCase();
    const sel = document.getElementById('categorySelect');
    if (!sel) return;
    for (let i=0;i<sel.options.length;i++){
        const o = sel.options[i];
        const text = (o.text||'').toLowerCase();
        o.style.display = q === '' || text.indexOf(q) !== -1 ? '' : 'none';
    }
});
</script>
